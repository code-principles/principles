# AR-002 — Explicitly declare dependencies

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Applies-to:** all

## Principle

An application must explicitly declare all of its dependencies through a dependency declaration manifest and must not rely on the implicit existence of system-wide packages. A twelve-factor app never assumes that libraries, tools, or system packages are pre-installed on the host. Dependency isolation (e.g., virtual environments, containers) ensures the application does not leak dependencies to or from the surrounding system.

## Why it matters

Implicit dependencies make applications fragile and non-portable. When an app assumes a specific system library or tool is installed, it works on the developer's machine but fails in clean environments. Explicit dependency declaration makes builds reproducible, onboarding straightforward, and deployment reliable across any target environment.

## Violations to detect

- Libraries or tools used at runtime that are not declared in the project's manifest (e.g., `package.json`, `requirements.txt`, `go.mod`, `pom.xml`)
- Shell scripts or build steps that rely on globally installed CLI tools without declaring or vendoring them
- Missing lock files (`package-lock.json`, `poetry.lock`, `Cargo.lock`) that pin exact dependency versions
- Dockerfile or build scripts that install packages not reflected in the application's dependency manifest
- System-level dependencies assumed but not documented or containerized

## Good practice

- Declare every dependency in the appropriate manifest file for the language/platform
- Use lock files to pin exact versions of all direct and transitive dependencies
- Use dependency isolation tools (virtual environments, containers, bundlers) to prevent system-wide leakage
- Automate the setup of a fresh development environment so that a new developer can go from clone to running with a single command
- Audit the dependency tree regularly and remove unused dependencies

## Sources

- Wiggins, Adam. "The Twelve-Factor App — II. Dependencies." https://12factor.net/dependencies
