# AR-005 — Strictly separate build, release, run

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Applies-to:** all

## Principle

A twelve-factor app uses strictly separated stages for build, release, and run. The build stage converts code into an executable bundle. The release stage combines the build with the deploy's configuration to produce a release ready for execution. The run stage launches the release in the execution environment. Each release should have a unique identifier (timestamp or incrementing number), and releases are append-only — any change creates a new release.

## Why it matters

Mixing these stages leads to unpredictable deployments. If code changes can be made at runtime, they cannot be tracked in version control and are easily lost on the next deploy. If configuration is baked into the build, the same artifact cannot be used across environments. Strict separation ensures reproducibility: any release can be audited, compared, or rolled back to.

## Violations to detect

- Code modified directly on production servers or in running containers
- Build artifacts that contain environment-specific configuration baked in at compile time
- No versioned or tagged releases — deployments are ad-hoc with no way to identify or roll back to a previous state
- The same process or script that builds the code also deploys and runs it without distinct phases
- Missing CI/CD pipeline with clearly separated build, package, and deploy stages

## Good practice

- Use a CI/CD pipeline with distinct build, release, and deploy steps
- Produce a single immutable build artifact that is promoted through environments without rebuilding
- Tag every release with a unique, monotonically increasing identifier
- Support instant rollback by keeping previous releases available and switching to them via configuration
- Never modify code or artifacts after they leave the build stage

## Sources

- Wiggins, Adam. "The Twelve-Factor App — V. Build, release, run." https://12factor.net/build-release-run
