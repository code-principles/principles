# AR-003 — Store config in the environment

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Applies-to:** all

## Principle

Store configuration that varies between deploys in environment variables, not in code. Config includes database URLs, credentials, per-environment feature flags, and external service endpoints. A twelve-factor app strictly separates config from code — the same build artifact must be deployable to any environment by changing only the config. A useful litmus test: could the codebase be made open-source at any moment without exposing credentials?

## Why it matters

Hardcoded configuration forces rebuilding or branching for each environment, creating drift and risk. Credentials in source code are a leading cause of security breaches, especially when repositories are accidentally made public or shared with contractors. Environment-based config enables the same artifact to flow from development through staging to production without modification.

## Violations to detect

- Database connection strings, API keys, or credentials hardcoded in source files
- Environment-specific values (URLs, feature flags, hostnames) in checked-in config files without override mechanism
- Config files organized by environment name (e.g., `config/production.yml`) that bundle together unrelated settings
- Secrets committed to version control, even in "private" repositories
- Build-time environment selection (e.g., compile-time `#ifdef PRODUCTION`) instead of runtime configuration

## Good practice

- Read configuration from environment variables at application startup
- Use a `.env` file for local development but never commit it to version control (add to `.gitignore`)
- Group related config values logically but keep them as independent, granular environment variables
- Use a secrets manager (e.g., Vault, AWS Secrets Manager, Azure Key Vault) for sensitive values in production
- Validate all required configuration at startup and fail fast with a clear error message if anything is missing

## Sources

- Wiggins, Adam. "The Twelve-Factor App — III. Config." https://12factor.net/config
