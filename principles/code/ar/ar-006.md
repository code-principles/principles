# AR-006 — Execute as stateless processes

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Applies-to:** all

## Principle

Twelve-factor processes are stateless and share-nothing. Any data that needs to persist must be stored in a stateful backing service such as a database. The process's memory and filesystem should be treated as ephemeral, single-transaction caches — nothing stored in memory or on disk is guaranteed to be available on a future request. Sticky sessions are a violation of this factor and should never be used.

## Why it matters

Stateful processes prevent horizontal scaling because requests must be routed to a specific instance. They also make deployments fragile: restarting or replacing a process loses state, causing data loss or broken user sessions. Stateless processes can be started, stopped, moved, and scaled freely, enabling zero-downtime deployments and elastic scaling.

## Violations to detect

- In-memory session state or caches that are assumed to survive across requests without a backing store
- Sticky sessions (session affinity) configured in the load balancer
- Local filesystem used for persistent storage (uploaded files, generated reports, caches) rather than an external service
- Application startup that depends on state left behind by a previous run of the same process
- Shared mutable state between processes via the local filesystem

## Good practice

- Store session data in a shared backing service (Redis, database) so any process instance can serve any request
- Use object storage (S3, GCS, Azure Blob) for file uploads and generated assets
- Design processes to start from scratch without depending on prior local state
- Use distributed caches (Redis, Memcached) rather than in-process caches for data that must be shared across instances
- Design for horizontal scaling from the start — assume multiple process instances running concurrently

## Sources

- Wiggins, Adam. "The Twelve-Factor App — VI. Processes." https://12factor.net/processes
