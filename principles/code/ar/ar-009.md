# AR-009 — Maximize robustness with fast startup and graceful shutdown

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Applies-to:** all

## Principle

Twelve-factor app processes should start quickly and shut down gracefully. Minimizing startup time enables rapid elastic scaling and fast deployment. Graceful shutdown means that when a process receives a termination signal (SIGTERM), it stops accepting new work, finishes in-flight requests, and then exits. For worker processes, this means returning the current job to the queue. Processes should also be robust against sudden death (hardware failure), which requires idempotent, re-entrant job design.

## Why it matters

Slow startup delays deployments, scaling events, and crash recovery. Abrupt shutdown causes dropped requests, lost work, and data corruption. When processes start fast and stop cleanly, the system can deploy, scale, and recover from failures without user-visible impact. Disposability is a prerequisite for robust zero-downtime deployments and elastic auto-scaling.

## Violations to detect

- Startup sequences that take more than a few seconds due to large caches being warmed, expensive initializations, or sequential connection setup
- Processes that immediately terminate on SIGTERM without draining in-flight requests
- Worker processes that lose jobs in progress on shutdown instead of returning them to the queue
- Non-idempotent job processing that cannot safely be retried after unexpected termination
- Missing health check endpoints that would allow orchestrators to detect readiness

## Good practice

- Minimize startup time by deferring expensive initialization and using lazy loading where appropriate
- Handle SIGTERM by stopping new request acceptance, completing in-flight work, and then exiting
- Set a reasonable shutdown deadline (e.g., 30 seconds) after which the process force-exits
- Design background jobs to be idempotent and re-entrant so they can be safely retried
- Use transactions or atomic operations to prevent partial state changes on unexpected termination
- Implement readiness probes so the orchestrator does not route traffic until the process is fully ready

## Sources

- Wiggins, Adam. "The Twelve-Factor App — IX. Disposability." https://12factor.net/disposability
