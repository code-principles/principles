# SD-005 — Dependency Inversion Principle

**Layer:** 2 (contextual)
**Categories:** software-design, object-oriented, maintainability
**Applies-to:** all

## Principle

High-level modules should not depend on low-level modules; both should depend on abstractions. Abstractions should not depend on details; details should depend on abstractions. The flow of source-code dependency must point against the flow of control, so that policy-level code is insulated from the mechanisms that implement it.

## Why it matters

When high-level business logic directly depends on low-level infrastructure (databases, file systems, APIs), changes in infrastructure ripple into the core domain. Inverting the dependency direction makes the domain portable, testable in isolation, and resilient to technology changes at the periphery.

## Violations to detect

- A business-logic class that directly instantiates a database connection, HTTP client, or file-system accessor
- Import or require statements in domain modules that reference infrastructure frameworks or libraries
- High-level policy modules that cannot be tested without standing up a database or network service
- Concrete class names used as parameter types where an interface or abstract type would suffice

## Good practice

- Define interfaces or abstract types in the high-level module and have low-level modules implement them
- Use dependency injection (constructor, parameter, or framework-based) to supply concrete implementations
- Structure projects so that the domain or application core has no outward-facing dependencies — infrastructure adapters depend inward
- In languages without interfaces, use protocols, abstract base classes, or function signatures as the abstraction boundary

## Sources

- Martin, Robert C. *Agile Software Development: Principles, Patterns, and Practices*. Pearson, 2003. ISBN 978-0-13-597444-5. Chapter 11.
- Martin, Robert C. *Clean Architecture*. Prentice Hall, 2017. ISBN 978-0-13-449416-6. Chapter 11.
