# SEC-011 — Prevent server-side request forgery

**Layer:** 2 (contextual)
**Categories:** security
**Applies-to:** all

## Principle

Never allow user-supplied data to control server-side network requests without strict validation and restriction. Server-Side Request Forgery (SSRF, A10:2021) occurs when an application fetches a remote resource using a URL provided or influenced by the user without validating the destination. Attackers exploit SSRF to access internal services, cloud metadata endpoints, and other resources that are not directly reachable from the internet.

## Why it matters

SSRF attacks bypass firewalls and network segmentation by using the application server as a proxy. In cloud environments, SSRF can reach instance metadata services (e.g., `169.254.169.254`) to steal IAM credentials, leading to full cloud account compromise. The Capital One breach demonstrated the severity of SSRF when combined with overly permissive cloud IAM roles. As architectures grow more complex with microservices and cloud APIs, SSRF attack surface expands.

## Violations to detect

- User-supplied URLs passed directly to HTTP client libraries without validation
- URL validation that only checks the scheme but not the host or IP address
- Allowlist bypasses through DNS rebinding, URL encoding tricks, or redirect chains
- Server-side fetches to internal networks or cloud metadata endpoints triggered by user input
- Webhook or integration features that allow users to specify arbitrary callback URLs
- Missing network-level segmentation between application servers and sensitive internal services

## Good practice

- Validate and sanitize all user-supplied URLs against a strict allowlist of permitted domains and protocols
- Deny requests to private and reserved IP ranges (10.x, 172.16-31.x, 192.168.x, 169.254.x, 127.x, ::1) at the application level
- Use network-level controls (firewall rules, security groups) to restrict outbound traffic from application servers
- Disable HTTP redirects in server-side HTTP clients, or re-validate the destination after each redirect
- For cloud workloads, use IMDSv2 or equivalent protections that require session tokens for metadata access
- Isolate URL-fetching functionality in a sandboxed service with minimal network access

## Sources

- OWASP Foundation. "OWASP Top 10:2021 — A10:2021 Server-Side Request Forgery (SSRF)." https://owasp.org/Top10/A10_2021-Server-Side_Request_Forgery_%28SSRF%29/
