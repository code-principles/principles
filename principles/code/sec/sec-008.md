# SEC-008 — Implement proper authentication and session management

**Layer:** 2 (contextual)
**Categories:** security
**Applies-to:** all

## Principle

Correctly verify user identity and protect sessions throughout their lifecycle. Identification and Authentication Failures (A07:2021) cover weaknesses such as allowing weak passwords, improper credential storage, missing multi-factor authentication, session fixation, and exposed session identifiers. Authentication is the gateway to the application — if it fails, access control is meaningless.

## Why it matters

Broken authentication gives attackers direct access to user accounts and the data and actions those accounts control. Credential stuffing attacks exploit weak password policies and missing brute-force protections. Session hijacking through insecure token handling lets attackers impersonate legitimate users. These attacks are heavily automated and target every internet-facing application.

## Violations to detect

- No rate limiting or account lockout on login endpoints, enabling brute-force and credential stuffing attacks
- Session tokens exposed in URLs or logs
- Session IDs not rotated after login, enabling session fixation
- Missing or weak password complexity requirements
- Passwords stored in plain text or with reversible encryption
- Authentication tokens (JWTs, API keys) that never expire or lack proper invalidation
- Missing multi-factor authentication on administrative or high-privilege accounts

## Good practice

- Use well-tested authentication frameworks and libraries rather than custom implementations
- Enforce multi-factor authentication, especially for admin and sensitive operations
- Hash passwords with modern algorithms (Argon2id, bcrypt, scrypt) with appropriate work factors
- Rotate session identifiers on login and privilege changes; set proper cookie flags (Secure, HttpOnly, SameSite)
- Implement rate limiting, progressive delays, or CAPTCHA on authentication endpoints
- Set reasonable session timeouts and provide explicit logout functionality that invalidates server-side session state
- Never expose session tokens or credentials in URLs, logs, or error messages

## Sources

- OWASP Foundation. "OWASP Top 10:2021 — A07:2021 Identification and Authentication Failures." https://owasp.org/Top10/A07_2021-Identification_and_Authentication_Failures/
