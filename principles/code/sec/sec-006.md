# SEC-006 — Eliminate security misconfigurations

**Layer:** 2 (contextual)
**Categories:** security
**Applies-to:** all

## Principle

Applications must be configured securely at every layer of the stack — application server, framework, database, cloud services, containers, and network. Security Misconfiguration (A05:2021) occurs when default credentials are left in place, unnecessary features are enabled, error messages reveal internal details, security headers are missing, or permissions are overly permissive. A secure application on a misconfigured platform is still vulnerable.

## Why it matters

Misconfiguration is one of the most common and easily exploitable vulnerability classes. Attackers routinely scan for default credentials, open cloud storage buckets, exposed admin panels, and verbose error pages. A single misconfigured component can negate all other security measures in the application.

## Violations to detect

- Default credentials or API keys left unchanged in production
- Unnecessary features, ports, services, or sample applications enabled in production
- Verbose error messages or stack traces exposed to end users
- Missing security headers (Content-Security-Policy, X-Content-Type-Options, Strict-Transport-Security)
- Overly permissive CORS, file permissions, or IAM policies
- Debug mode enabled in production environments
- Cloud storage buckets or database instances publicly accessible without authentication

## Good practice

- Establish a repeatable hardening process for all environments; automate it with infrastructure-as-code
- Maintain a minimal platform — remove or disable every feature, component, and service that is not needed
- Use different credentials for every environment and rotate them regularly
- Implement security headers and review them with each release
- Run automated configuration scanners (e.g., CIS Benchmarks, cloud provider security tools) in CI/CD
- Review and restrict permissions following the principle of least privilege at every layer

## Sources

- OWASP Foundation. "OWASP Top 10:2021 — A05:2021 Security Misconfiguration." https://owasp.org/Top10/A05_2021-Security_Misconfiguration/
