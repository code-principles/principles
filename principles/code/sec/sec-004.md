# SEC-004 — Prevent injection attacks

**Layer:** 2 (contextual)
**Categories:** security
**Applies-to:** all

## Principle

Never allow untrusted data to be interpreted as commands or queries. Injection (A03:2021) occurs when user-supplied data is sent to an interpreter — SQL engine, OS shell, LDAP directory, XPath evaluator, or others — without proper separation of code and data. The primary defense is to use parameterized interfaces that keep data and instructions in separate channels.

## Why it matters

Injection attacks consistently rank among the most dangerous web vulnerabilities. A successful SQL injection can dump or destroy an entire database. Command injection can give an attacker full control of the server. Cross-site scripting (XSS), now grouped under this category, can hijack user sessions and steal credentials. These attacks are well-understood and preventable, making their presence inexcusable.

## Violations to detect

- String concatenation or interpolation used to build SQL queries, OS commands, or LDAP queries
- User input embedded directly into HTML output without context-appropriate encoding (XSS)
- Dynamic code evaluation with user-controlled input (`eval()`, `exec()`, template injection)
- ORM queries built with raw string fragments containing user input
- Deserialization of untrusted data without type constraints

## Good practice

- Use parameterized queries (prepared statements) for all database access
- Use an ORM's safe query-building API; avoid raw query methods with string interpolation
- Apply context-specific output encoding (HTML, JavaScript, URL, CSS) when rendering user data
- Use allowlist validation on inputs that must match a specific pattern (e.g., identifiers, enum values)
- Run applications with least-privilege database and OS accounts to limit blast radius
- Employ static analysis tools that detect injection patterns during code review and CI

## Sources

- OWASP Foundation. "OWASP Top 10:2021 — A03:2021 Injection." https://owasp.org/Top10/A03_2021-Injection/
