# SEC-002 — Prevent broken access control

**Layer:** 2 (contextual)
**Categories:** security
**Languages:** all

## Principle

Every request must enforce access control checks so that users can only act within their intended permissions. Access control fails when the application does not verify that the authenticated user is authorized to perform the requested action on the requested resource. OWASP ranks Broken Access Control as the number one web application security risk (A01:2021) because it is both widespread and high-impact.

## Why it matters

When access control is missing or inconsistent, attackers can view other users' data, modify records they should not touch, or escalate their privileges to administrator level. These failures lead to data breaches, unauthorized transactions, and full system compromise. Access control bugs are rarely caught by automated scanners and often require deliberate review.

## Violations to detect

- Endpoints that rely solely on client-side checks or hidden UI elements for authorization
- Missing authorization checks on API endpoints, especially CRUD operations that accept a resource ID
- Insecure direct object references (IDOR) where changing an ID in the URL exposes another user's data
- Elevation of privilege by modifying JWT claims, cookies, or hidden form fields
- CORS misconfiguration allowing access from unauthorized origins
- Missing access control on file uploads, metadata endpoints, or admin functions

## Good practice

- Deny by default — require explicit grants for every action and resource
- Enforce authorization server-side on every request, regardless of the client path taken
- Use parameterized, centralized access control mechanisms rather than scattering checks throughout the code
- Implement rate limiting and logging on access control failures to detect attacks
- Disable directory listing on web servers and ensure metadata files (e.g., `.git`) are not accessible
- Test access control with automated integration tests that attempt cross-user and privilege escalation scenarios

## Sources

- OWASP Foundation. "OWASP Top 10:2021 — A01:2021 Broken Access Control." https://owasp.org/Top10/A01_2021-Broken_Access_Control/
