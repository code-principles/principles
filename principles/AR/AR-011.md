# AR-011 — Treat logs as event streams

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Languages:** all

## Principle

A twelve-factor app never concerns itself with routing or storing its own log output. It treats logs as a time-ordered stream of events and writes them, unbuffered, to stdout. The execution environment is responsible for capturing, collating, routing, and archiving the log stream. The app should not write to log files, manage log rotation, or concern itself with the destination of its output.

## Why it matters

When applications manage their own log files, they create operational burden (disk space management, rotation, permissions) and limit observability. Log files on individual containers or ephemeral instances are lost when the instance terminates. Writing to stdout enables the execution environment or a log aggregation system to centrally collect, index, search, and alert on logs from all instances, enabling real-time visibility and historical analysis.

## Violations to detect

- Application code that writes logs to specific file paths rather than stdout/stderr
- Log rotation logic implemented within the application
- Logging frameworks configured to write to files in production rather than console output
- Log output suppressed or discarded rather than emitted to the standard streams
- Custom log shipping code embedded in the application instead of relying on the platform

## Good practice

- Configure logging frameworks to write to stdout (and stderr for errors) in all environments
- Use structured logging (JSON format) so logs can be parsed and indexed by aggregation systems
- Let the execution environment (container runtime, PaaS, or log agent) capture and route the log stream
- Use a centralized log aggregation service (ELK, Splunk, Datadog, CloudWatch) for searching, alerting, and analysis
- Include correlation IDs in log entries to trace requests across services

## Sources

- Wiggins, Adam. "The Twelve-Factor App — XI. Logs." https://12factor.net/logs
