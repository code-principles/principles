# AR-004 — Treat backing services as attached resources

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Languages:** all

## Principle

Treat every backing service — database, message queue, cache, SMTP server, external API — as an attached resource accessed via a URL or locator stored in configuration. A twelve-factor app makes no distinction between local and third-party services; both are attached resources that can be swapped by changing configuration without code changes. A locally managed PostgreSQL instance and an Amazon RDS instance should be interchangeable by changing the connection URL.

## Why it matters

When applications are tightly coupled to a specific service instance, replacing a failed database, migrating to a managed service, or switching providers requires code changes and redeployment. Treating services as attached resources enables operational flexibility: a failing database can be detached and a restored one attached, all through configuration rather than code modification.

## Violations to detect

- Service hostnames, ports, or connection details hardcoded in application code
- Direct filesystem paths used for services that should be accessed through an abstraction (e.g., local file storage instead of an object storage API)
- Code that assumes a specific vendor or provider for a backing service without an abstraction layer
- Initialization code that creates or configures infrastructure rather than consuming it through config

## Good practice

- Access every backing service through a URL or connection string read from environment configuration
- Ensure each backing service can be swapped by changing a single configuration value without code changes
- Use abstraction layers or adapters that decouple application logic from specific service implementations
- Treat local development services (local Redis, local Postgres) identically to production ones — same code path, different config

## Sources

- Wiggins, Adam. "The Twelve-Factor App — IV. Backing Services." https://12factor.net/backing-services
