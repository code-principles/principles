# AR-008 — Scale out via the process model

**Layer:** 2 (contextual)
**Categories:** architecture, cloud-native, deployment
**Languages:** all

## Principle

Scale an application by running more processes, not by making individual processes larger. A twelve-factor app uses the Unix process model: different types of work are assigned to different process types (e.g., web processes handle HTTP requests, worker processes handle background jobs), and each type can be independently scaled by adding more instances. The app should never daemonize or write PID files — it relies on the operating system's process manager or orchestrator for lifecycle management.

## Why it matters

Vertical scaling (bigger machines) has hard limits and creates single points of failure. Horizontal scaling through multiple processes enables elastic capacity, fault tolerance, and workload isolation. When web and worker workloads can be scaled independently, resources are allocated efficiently — a spike in background job volume does not require over-provisioning the web tier.

## Violations to detect

- An application that spawns its own daemon processes or manages its own process lifecycle (PID files, double-forking)
- Monolithic architectures where all workload types (HTTP, background jobs, scheduled tasks) run in a single process with no ability to scale them independently
- Vertical-only scaling strategies that rely on adding CPU or memory to a single instance
- Thread-based concurrency used as a substitute for the process model when work types need independent scaling
- In-process background job queues that cannot be distributed across multiple instances

## Good practice

- Decompose the application into process types (web, worker, scheduler, etc.) based on workload characteristics
- Use a process manager or container orchestrator (systemd, Kubernetes, Nomad) to manage process instances
- Scale each process type independently based on its specific load characteristics
- Use message queues or job brokers to distribute work across worker process instances
- Design each process to be stateless (Factor VI) so instances are interchangeable

## Sources

- Wiggins, Adam. "The Twelve-Factor App — VIII. Concurrency." https://12factor.net/concurrency
