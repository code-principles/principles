# SD-004 — Interface Segregation Principle

**Layer:** 2 (contextual)
**Categories:** software-design, object-oriented, maintainability
**Languages:** all

## Principle

Clients should not be forced to depend on methods they do not use. Rather than one large, general-purpose interface, define smaller, role-specific interfaces so that implementing classes only need to know about the methods that are relevant to them. Fat interfaces create coupling between clients that have no business knowing about each other.

## Why it matters

When a class is forced to implement interface methods it does not need, changes to those unrelated methods can trigger recompilation, redeployment, and retesting of code that should be unaffected. Large interfaces also increase the cognitive burden on implementors and invite degenerate implementations that throw "not supported" exceptions.

## Violations to detect

- An interface with many methods where most implementors leave several as no-ops or throw `NotImplementedException`
- A class that depends on a wide interface but only calls one or two of its methods
- A change to one method signature in an interface forces modifications in classes that never use that method
- Marker methods or feature-flag methods added to an existing interface to support a single consumer

## Good practice

- Split fat interfaces into cohesive, client-specific interfaces (e.g., `Readable`, `Writable` instead of a single `ReadWrite`)
- Apply the principle at module and API boundaries, not just at the class level
- Use interface composition: a class can implement multiple small interfaces
- In dynamically typed languages, rely on protocols or structural typing to achieve the same separation naturally

## Sources

- Martin, Robert C. *Agile Software Development: Principles, Patterns, and Practices*. Pearson, 2003. ISBN 978-0-13-597444-5. Chapter 12.
